type Mutation {
  createUser(firstName: String!, lastName: String!, middleName: String!, email: String!, password: String!): User
}

type Query {
  fetchUsers: [User!]
}

type User {
  firstName: String!
  lastName: String!
  middleName: String!
  email: String!
}