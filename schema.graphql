type Mutation {
  createUser(firstName: String!, lastName: String! email: String!): User
}

type Query {
  fetchUsers: User
}

type User {
  id: ID!
  email: String
  firstName: String!
  lastName: String!
}